version: '3.3'
services:
  moodist:
    image: ghcr.io/asherbythesea/moodist-spatial:latest
    build: .
    container_name: moodist-server
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped
